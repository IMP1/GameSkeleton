package lib.gui;

public class WindowTitle extends WindowResizable {
	
	public WindowTitle(int x, int y, int initialWidth, int initialHeight, int minWidth, int minHeight, int maxWidth, int maxHeight) {
		super(x, y, initialWidth, initialHeight, minWidth, minHeight, maxWidth, maxHeight);
	}

}
